# API Contracts
هر سرویس به صورت خودکار Swagger/OpenAPI دارد:
- `/docs` برای Swagger UI
- `/openapi.json` برای فایل OpenAPI

برای ارائه به عنوان PDF:
1) به `/openapi.json` بروید و فایل را ذخیره کنید
2) با ابزارهایی مثل `redocly` یا `swagger2pdf` خروجی PDF بگیرید.
